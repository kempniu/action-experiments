name: Test branch
on: push

jobs:
  alpine:
    runs-on: ubuntu-20.04
    container:
      image: alpine:3.14
    name: Check /etc/issue
    steps:
      - name: List contents of /etc/issue
        run: cat /etc/issue
